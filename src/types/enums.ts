export enum Theme {
  dark = "dark",
  light = "light",
}

export enum ThemeMainColor {
  dark = "#25262A",
  light = "#fff",
}

export enum Status {
  Todo = "todo",
  Doing = "doing",
  Done = "done",
}

export enum StatusLabel {
  todo = "TO-DO",
  doing = "DOING",
  done = "DONE",
}

export enum Mode {
  create = "create",
  edit = "edit",
}
